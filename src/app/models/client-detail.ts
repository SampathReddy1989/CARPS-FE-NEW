export interface ClientDetail {
    active?: string;
    clientId?: number;
    clientName?: string;
    timeZone?: string;

    //ui variable
    uiActive?: boolean;
    configurationFlag?: string;
}
